import './App.css'

function App() {
  return (
    <main className="app">
      <section className="hero">
        <span className="eyebrow">VIPER Forensic Engine</span>
        <h1>Frontend scaffold initialized.</h1>
        <p>
          This Vite + React workspace is ready for the live upload demo and
          will be expanded in the next frontend commits.
        </p>
      </section>
    </main>
  )
}

export default App
